using NUnit.Framework;

namespace UnitTestProject1
{
    [TestFixture]
    public class UnitTest1
    {
        [Test]
        public void TestMethod1()
        {


          /*  var repository = new Mock<IPostRepository>();
            Post post = new Post();
            repository.Setup(r => r.Read(1)).Returns(post);
            UserController userController = new UserController(repository.Object);
            var result = new Post();
            Assert.AreEqual(result, repository.Object);
            */

        }

    }
}
